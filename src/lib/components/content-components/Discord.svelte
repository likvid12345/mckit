<div class="discord-wrapper">
	<div class="discord-card">
		<div class="card-header">
			<div>
				<span class="eyebrow">COMMUNITY</span>
				<h1>mckit Discord</h1>
			</div>

			<div class="discord-mark">DISCORD</div>
		</div>

		<div class="divider"></div>

		<p>Feel free to join our Discord server. We talk about all sorts of things there :)</p>

		<a href="https://discord.gg/KwE5EQg4a" target="_blank" rel="noopener noreferrer">
			Join Discord
		</a>
	</div>
</div>

<style>
	.discord-wrapper {
		padding: 1rem;
	}

	.discord-card {
		max-width: 700px;
		padding: 1.5rem;
		border: 1px solid rgba(255, 255, 255, 0.08);
		border-radius: 12px;
		background: rgba(255, 255, 255, 0.025);
	}

	.card-header {
		display: flex;
		align-items: flex-start;
		justify-content: space-between;
		gap: 1rem;
	}

	.eyebrow {
		display: block;
		margin-bottom: 0.35rem;
		color: var(--secondary);
		font-size: 0.7rem;
		font-weight: 700;
		letter-spacing: 0.12em;
	}

	h1 {
		margin: 0;
		color: white;
		font-size: 1.7rem;
		line-height: 1.2;
	}

	.discord-mark {
		padding: 0.3rem 0.65rem;
		border: 1px solid color-mix(in srgb, var(--secondary) 50%, transparent);
		border-radius: 6px;
		background: color-mix(in srgb, var(--secondary) 15%, transparent);
		color: var(--secondary);
		font-size: 0.65rem;
		font-weight: 700;
		letter-spacing: 0.08em;
	}

	.divider {
		height: 1px;
		margin: 1.25rem 0;
		background: rgba(255, 255, 255, 0.07);
	}

	p {
		max-width: 600px;
		margin: 0 0 1.25rem;
		color: rgba(255, 255, 255, 0.65);
		font-size: 0.95rem;
		line-height: 1.7;
	}

	a {
		display: inline-flex;
		align-items: center;
		gap: 0.6rem;
		padding: 0.55rem 1rem;
		border: 1px solid color-mix(in srgb, var(--secondary) 60%, transparent);
		border-radius: 7px;
		background: var(--secondary);
		color: white;
		font-size: 0.9rem;
		font-weight: 700;
		text-decoration: none;
		transition:
			transform 0.15s ease,
			opacity 0.15s ease;
	}
</style>
